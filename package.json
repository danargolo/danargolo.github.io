{
  "name": "Project_BlogAssembler",
  "version": "1.0.0",
  "description": "Descrição do seu projeto",
  "main": "index.js",
  "scripts": {
    "deploy": "npm run format && npm run git-pull && npm run git-commit-push",
    "git-pull": "git pull origin main",
    "git-commit-push": "git commit -am 'Post Deploy' && git push -u origin main",
    "format": "prettier --write \"**/*.html\""
  },
  "author": "Danilo Argolo",
  "license": "MIT",
  "devDependencies": {
    "prettier": "3.1.0"
  }
}
